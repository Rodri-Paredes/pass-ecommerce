import { useEffect } from 'react';
import { Link } from 'react-router-dom';
import { ArrowLeft, Crown } from 'lucide-react';
import { usePassCrewStore } from '../store/passCrewStore';
import { useCustomerAuthStore } from '../store/customerAuthStore';
import RequestStatusBadge from '../components/pass-crew/RequestStatusBadge';

export default function PassCrewStatusPage(){const{customer}=useCustomerAuthStore();const{membership,scheduledMembership,requests,isLoading,loadMyStatus}=usePassCrewStore();useEffect(()=>{if(customer)loadMyStatus(customer.id)},[customer,loadMyStatus]);return <div className="min-h-screen bg-[#f5f5f2] px-4 pb-20 pt-28"><div className="mx-auto max-w-3xl"><Link to="/account" className="mb-8 inline-flex items-center gap-2 text-xs uppercase tracking-[.2em] text-black/50"><ArrowLeft className="h-4 w-4"/>Mi cuenta</Link><h1 className="text-4xl font-black uppercase tracking-[-.04em] sm:text-6xl">PASS Crew</h1>{membership&&<div className="mt-9 bg-black p-8 text-white"><Crown className="h-6 w-6 text-champagne"/><p className="mt-5 text-xs uppercase tracking-[.25em] text-white/45">Membresía activa</p><p className="mt-2 text-3xl font-black">{membership.plan_name_snapshot}</p><p className="mt-3 text-sm text-white/60">{membership.member_number} · vence {new Date(membership.expires_at).toLocaleDateString('es-BO')}</p>{scheduledMembership&&<p className="mt-4 border-t border-white/15 pt-4 text-sm text-champagne">Renovación {scheduledMembership.plan_name_snapshot} programada desde {new Date(scheduledMembership.started_at).toLocaleDateString('es-BO')}</p>}</div>}<div className="mt-6 bg-white p-6"><h2 className="text-xs font-bold uppercase tracking-[.2em] text-black/45">Solicitudes</h2>{isLoading?<p className="mt-4 text-sm text-black/40">Cargando…</p>:requests.length===0?<p className="mt-4 text-sm text-black/40">No tienes solicitudes.</p>:<div className="mt-4 divide-y">{requests.map(row=><div key={row.id} className="flex items-center justify-between gap-4 py-4"><div><p className="font-mono text-sm font-bold">{row.request_number}</p><p className="mt-1 text-xs text-black/45">{row.plan_name_snapshot} · Bs {Number(row.price_snapshot).toFixed(0)} · {new Date(row.created_at).toLocaleDateString('es-BO')}</p>{row.rejection_reason&&<p className="mt-1 text-xs text-red-600">{row.rejection_reason}</p>}</div><RequestStatusBadge status={row.status}/></div>)}</div>}</div>{!membership&&!requests.some(r=>r.status==='draft'||r.status==='pending')&&<Link to="/pass-crew/join" className="mt-6 block bg-black py-4 text-center text-xs font-bold uppercase tracking-[.2em] text-white">Nueva solicitud</Link>}</div></div>}
